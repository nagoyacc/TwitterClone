<h3>以下の内容で送信する。</h3>
<%= form_with(model: @post, url: choose_new_or_edit, local: true) do |form| %>
  <%= form.hidden_field :content %>
  <p>本文:<%= @post.content %></p>
  <%= form.submit  "送信" %>
<% end %>
<%= link_to '戻る', :back %>
